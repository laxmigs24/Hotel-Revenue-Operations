# Core data & ML
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
joblib>=1.3

# Visualization (notebooks)
matplotlib>=3.7
seaborn>=0.12

# App
streamlit>=1.30
plotly>=5.18

# Feature importance / explainability
# NOTE: this project was built and verified end-to-end using
# sklearn.inspection.permutation_importance, which has no extra
# dependency. SHAP is optional -- the app degrades gracefully
# (SHAP_AVAILABLE=False) and falls back to rule-based risk drivers if
# this isn't installed. Uncomment if you want SHAP-based explanations:
# shap>=0.44

# Testing
pytest>=7.4

# Optional: if you want to swap the HistGradientBoostingClassifier used
# in 04_modeling_evaluation.ipynb back to XGBoost/LightGBM (the libraries
# implied by the original project's filename `xgboost_cancellation_model_bundle`),
# install one of:
# xgboost>=2.0
# lightgbm>=4.0
